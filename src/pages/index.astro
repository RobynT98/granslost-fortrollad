---
import { getCollection } from 'astro:content';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import BaseHead from '../../components/BaseHead.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());
---

<html lang="sv">
  <head>
    <BaseHead title={`Blogg — ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main class="container">
      <h1>Blogg</h1>
      <ul class="post-list">
        {posts.map((p) => (
          <li class="post">
            <a href={`/blogg/${p.slug}/`} class="title">{p.data.title}</a>
            <div class="meta">
              {p.data.pubDate.toLocaleDateString('sv-SE')}
            </div>
            <p class="desc">{p.data.description}</p>
          </li>
        ))}
      </ul>
    </main>
    <Footer />
  </body>
</html>

<style>
  .container { max-width: 980px; margin: 0 auto; padding: 2rem 1rem; }
  .post-list { list-style: none; padding: 0; margin: 1rem 0; }
  .post { padding: 1rem 0; border-bottom: 1px solid #ececf1; }
  .post:last-child { border-bottom: 0; }
  .title { font-weight: 700; text-decoration: none; color: var(--ink, #101014); }
  .title:hover { color: var(--brand, #8B0000); }
  .meta { font-size: .9rem; color: #666; margin: .25rem 0; }
  .desc { margin: .25rem 0 0; color: #444; }
</style>
