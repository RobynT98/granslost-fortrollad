<style>
  .hero {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 1rem;
  }
  .card {
    background: white;
    border-radius: 12px;
    box-shadow: var(--box-shadow, 0 12px 40px rgba(0,0,0,.12));
    padding: 2rem;
    max-width: 32rem;
    margin: auto;
  }
  .title {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: .5rem;
  }
  .title img { width: 72px; height: 72px; }
  h1 { margin: .5rem 0 1rem; font-size: 2rem; }
  p { margin: .5rem 0; }
  .btn {
    display: inline-block;
    margin-top: 1rem;
    padding: .75rem 1.5rem;
    background: var(--accent, #c1121f);
    color: #fff;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 700;
    transition: background .2s ease;
  }
  .btn:hover { background: #a60000; }
  .btn:visited { color: #fff; }
</style>
